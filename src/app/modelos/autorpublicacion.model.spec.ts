import { Autorpublicacion } from './autorpublicacion.model';

describe('Autorpublicacion', () => {
  it('should create an instance', () => {
    expect(new Autorpublicacion()).toBeTruthy();
  });
});
